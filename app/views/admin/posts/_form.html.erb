<%= form_for [:admin, @post], multipart: true do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <div class="row justify-content-center">
    <div class="col-md-11">
      <div class="rounded" id="background-preview" style="background-image: url(<%= asset_path background_url(@post) %>);">
        <%= f.file_field :cover %>
      </div>
      <div class="form-group" style="margin-top: 20px">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :content %>
        <%= f.text_area :content, class: 'form-control' %>
        <div class="stackedit-button-wrapper">
          <a id="edit-with-stackedit"><img src="<%= image_path 'stackedit.svg' %>" style="width: 20px">Edit with StackEdit</a>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :category %>
        <%= f.select :category, Post::CATEGORIES.each_with_index.map{|value, index| [value, index]}, {include_blank: 'Select from below'}, class: 'form-control'  %>
      </div>

      <div class="form-group">
        <%= label_tag :tag %>
        <ul id="form-tags">
          <% @post.tags.each do |tag| %>
            <li><%= tag.name %></li>
          <% end %>
        </ul>
      </div>

      <%= f.submit t('submit'), class: 'btn btn-primary form-control' %>
    </div>
  </div>
<% end %>